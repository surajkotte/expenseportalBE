version: '3.8'

services:
  db:
    image: mysql:latest
    container_name: expenseportal_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Otvim1234!
      MYSQL_DATABASE: expenseportal
      MYSQL_PASSWORD: Otvim1234!
    ports:
      - "3307:3306"
    volumes:
      - mysql_expenseportal:/var/lib/mysql

volumes:
  mysql_expenseportal: